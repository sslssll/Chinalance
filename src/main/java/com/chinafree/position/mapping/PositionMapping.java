package com.chinafree.position.mapping;

import org.mapstruct.Mapper;
import org.mapstruct.factory.Mappers;

@Mapper
public class PositionMapping {

    PositionMapping positionMapping = Mappers.getMapper(PositionMapping.class);



}
